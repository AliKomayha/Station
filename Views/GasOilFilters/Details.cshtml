@model Station.Models.GasOilFilter

<h2>Government Gas Oil Filters</h2>

<div>
    <p><strong>Type:</strong> @Model.Type</p>
    <p><strong>Quantity:</strong> @Model.Quantity</p>
    <p><strong>Price:</strong> @Model.Price</p>
    <p><strong>Date:</strong> @Model.Date.ToString("yyyy-MM-dd")</p>
    <p><strong>User:</strong> @Model.User?.Name</p>
</div>

<p>
    <a href="/GasOilFilters/Delete/@Model.Id" class="btn btn-danger">Delete</a>
    <a href="/GasOilFilters/Index" class="btn btn-secondary">Back to List</a>
    <a href="/GasOilFilters/Edit/@Model.Id" class="btn btn-primary">Edit</a>
</p>
