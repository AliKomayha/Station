@model IEnumerable<Station.Models.User>

@{
    ViewData["Title"] = "Users";
}

<h2>Users</h2>

<p>
    <a href="/User/Create">Create New User</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Role</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.Name</td>
                <td>@user.Role</td>
                <td>
                    <a href="/User/Details/@user.Id">Details</a> |
                    <a href="/User/Edit/@user.Id">Edit</a> |
                    <a href="/User/Delete/@user.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
