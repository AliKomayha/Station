@model IEnumerable<Station.Models.CarWash>

@{
    ViewData["Title"] = "Car Washes";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2 class="mb-0">
        <i class="fas fa-car me-2"></i>Car Washes
    </h2>
    <a href="/CarWashes/Create" class="btn btn-success">
        <i class="fas fa-plus me-1"></i>Create New Car Wash
    </a>
</div>

<div class="table-responsive">
    <table class="table table-striped table-hover">
        <thead class="table-dark">
            <tr>
                <th>Type</th>
                <th>Price</th>
                <th>Date</th>
                <th>User</th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var carwash in Model)
            {
                <tr>
                    <td>
                        @carwash.Type
                    </td>
                    <td>
                        <strong class="text-success">$@carwash.Price.ToString("F2")</strong>
                    </td>
                    <td>@carwash.Date.ToString("MMM dd, yyyy")</td>
                    <td>@carwash.User?.Name</td>
                    <td class="text-center">
                        <div class="btn-group" role="group">
                            <a href="/CarWashes/Details/@carwash.Id" class="btn btn-info btn-sm" title="View Details">
                                <i class="fas fa-eye"></i>
                            </a>
                            <a href="/CarWashes/Edit/@carwash.Id" class="btn btn-primary btn-sm" title="Edit">
                                <i class="fas fa-edit"></i>
                            </a>
                            <a href="/CarWashes/Delete/@carwash.Id" class="btn btn-danger btn-sm" title="Delete">
                                <i class="fas fa-trash"></i>
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
